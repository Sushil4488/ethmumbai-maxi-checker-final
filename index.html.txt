<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ETH Maxi Checker</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background: linear-gradient(135deg, #0f172a, #020617);
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background: #020617;
            border-radius: 12px;
            padding: 30px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 0 30px rgba(0,0,0,0.6);
            text-align: center;
        }

        h1 {
            margin-bottom: 10px;
            font-size: 26px;
        }

        p {
            color: #cbd5f5;
            font-size: 14px;
            margin-bottom: 20px;
        }

        input {
            width: 100%;
            padding: 14px;
            border-radius: 8px;
            border: none;
            outline: none;
            font-size: 16px;
            margin-bottom: 15px;
        }

        button {
            width: 100%;
            padding: 14px;
            background: #6366f1;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #4f46e5;
        }

        .result {
            margin-top: 20px;
            padding: 15px;
            border-radius: 8px;
            background: #020617;
            border: 1px solid #1e293b;
            display: none;
        }

        .footer {
            margin-top: 20px;
            font-size: 12px;
            color: #64748b;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>ETH Maxi Checker</h1>
    <p>Enter an address or username to check ETH Maxi status</p>

    <input type="text" id="inputValue" placeholder="Enter wallet address">
    <button onclick="checkStatus()">Check</button>

    <div class="result" id="resultBox"></div>

    <div class="footer">
        Built with ❤️ | Demo Version
    </div>
</div>

<script>
    function checkStatus() {
        const input = document.getElementById("inputValue").value.trim();
        const resultBox = document.getElementById("resultBox");

        if (input === "") {
            resultBox.style.display = "block";
            resultBox.innerHTML = "❌ Please enter a valid value.";
            return;
        }

        // Fake logic (demo purpose)
        const isMaxi = input.length % 2 === 0;

        resultBox.style.display = "block";
        resultBox.innerHTML = isMaxi
            ? "✅ This address is an ETH Maxi!"
            : "❌ Not an ETH Maxi (yet)";
    }
</script>

</body>
</html>
